

## npm

[source,shell]
----
npm ls      # 显示当前的依赖树

npm install -g available-versions
releases angular    # 查看 angular 发布的所有版本

# 查看全局安装了哪些组件/命令， `--depth=0` 仅显示顶层包，不含依赖树。
npm list -g --depth=0
----

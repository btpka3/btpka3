link:https://prismjs.com/[prismjs]

Prism 使用自定义的正则表达式解析，核心是一个约 2KB 的轻量引擎。它对常见语言覆盖不错，但遇到边缘语法时容易出现误判或着色不完整。



Prism 则是客户端高亮（CSR）的经典方案。页面加载后由浏览器执行正则匹配和 DOM 操作，适合内容动态生成、无法预渲染的场景（如在线代码编辑器、评论区代码块）。缺点是会增加主线程负担，大量代码块时可能出现卡顿。

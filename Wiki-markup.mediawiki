==表格==
{| width="90%"
|+
! 标记 !! 作用
|- 
| <nowiki>{|</nowiki> || 表格开始标记。须在行首。
|-
| <nowiki>|+</nowiki> || 表头行开始标记。须在行首。可选，只能位于表格开始标记和第一行之间。
|-
| <nowiki>|-</nowiki> || 行开始标记。须在行首。仅在第一行时可以没有。
|-
| <nowiki>!</nowiki>  || 表头单元格标记。须在行首。多个单元格写在一行是，第二个及以后的单元格需用<code>!!</code>开头
|- 
| <nowiki>|</nowiki>  || 在行首时，表示是数据单元格标记。在单元格内容之间时，（可选），第一个出现的位置将单元格内容划分为2部分，第一部分表示CSS式样，第二部分表示单元格内容。
|-
|  style="align:right"|<nowiki>|}</nowiki> || 表格结束标记。需单独占一行，且无前导空白。
|}

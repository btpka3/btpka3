


## alternative
```txt
# 查看 网络连接
netstat -a          # ss

# 查看 路由表
netstat -r          # ip route

# 统计接口
netstat -i          # ip -s link

# 伪连接
netstat -M          # ss

# 组播成员
netstat -g          # ip maddr
```
